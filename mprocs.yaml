procs:
  ngrock:
    cmd: ["cmd", "/c", "npm", "run", "ngrock:dev"]  # ✅ Fixed

  inngest:
    cmd: ["cmd", "/c", "npm", "run", "inngest:dev"]

  next:
    cmd: ["cmd", "/c", "npm", "run", "dev"]